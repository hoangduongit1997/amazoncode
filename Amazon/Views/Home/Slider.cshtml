@using Amazon.EntityFramework;
@model List<Slider>
<div id="myCarousel" class="carousel slide" data-ride="carousel">
    <ol class="carousel-indicators">

        @foreach (var item in Model)
        {
            <li data-target="#myCarousel" data-slide-to="@item.DisplayOrder.ToString()" class="@item.Priority"></li>
        }
    </ol>
    <div class="carousel-inner">
        @foreach (var item in Model)
        {
            <div class="item @item.Priority">
                <a href="#">
                    <img src="@item.Image" width="100%" alt="@item.Description" />
                </a>
            </div>
        }
        <a class="left carousel-control" href="#myCarousel" data-slide="prev">
            <span class="glyphicon glyphicon-chevron-left"></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="right carousel-control" href="#myCarousel" data-slide="next">
            <span class="glyphicon glyphicon-chevron-right"></span>
            <span class="sr-only">Next</span>
        </a>
    </div>
</div>

    @*<div id="myCarousel" class="carousel slide" data-ride="carousel">
        <!-- Indicators -->
        <ol class="carousel-indicators">
            <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
            <li data-target="#myCarousel" data-slide-to="1"></li>
            <li data-target="#myCarousel" data-slide-to="2"></li>
        </ol>

        <!-- Wrapper for slides -->
        <div class="carousel-inner">
            <div class="item active">
                <a href="#">
                    <img src="~/Upload/image/logo/panel1.PNG" width="100%" alt="Panel_1" />
                </a>
            </div>

            <div class="item">
                <a href="#">
                    <img src="~/Upload/image/logo/panel2.PNG" width="100%" alt="Panel_2" />
                </a>
            </div>

            <div class="item">
                <a href="#"><img src="~/Upload/image/logo/panel3.PNG" width="100%" alt="Panel_3" /></a>
            </div>
            <a class="left carousel-control" href="#myCarousel" data-slide="prev">
                <span class="glyphicon glyphicon-chevron-left"></span>
                <span class="sr-only">Previous</span>
            </a>
            <a class="right carousel-control" href="#myCarousel" data-slide="next">
                <span class="glyphicon glyphicon-chevron-right"></span>
                <span class="sr-only">Next</span>
            </a>
        </div>
    </div>*@
